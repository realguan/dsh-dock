# F3 修复报告：`sessions.rs` 临时脚本泄漏（task-13）

> 性质 = **fix**｜执行 = `rust-mgmt`｜日期 = 2026-09-11
> 基线 = task-12 收尾态（`cargo test` **292 passed / 3 ignored**）
> 来源 = task-12 顺带发现（`docs/team/F2会话目录名修复-2026-09-11.md` §5）
> 落点 = `docs/team/`（`docs/team/README.md` §2.5）

## 0. 一句话

两条「先写临时脚本、再跑 node」的链路，其清理语句排在**两个 `?` 早退之后**
（合计 **4 条泄漏出口**）——改为 **RAII guard** 后全部出口（含未来新增出口与
panic 展开）自动收口；实测**一轮 `cargo test` 的泄漏量由 +5 份/460 KB 降为 0**，
并回收本机存量 **~6.9 MB**。

## 1. 复现先行（红 → 绿，均为原文）

### 1.1 套件级复现（最强证据：修前跑一轮 test 的泄漏量）

```
=== 修前 BEFORE: n=63  5796KB ===
test result: ok. 292 passed; 0 failed; 3 ignored
=== 修前 AFTER : n=68  6256KB ===
=== 修前 一轮 cargo test 泄漏 delta = 5 份 / 460 KB ===
```

**含义**：不修的话，**每个开发者每跑一次 `cargo test` 就往 `$TMPDIR` 堆 5 份 ≈460 KB**
（单份 ≈92 KB = `scripts/repair-session.mjs` 全文）。这是本任务最有价值的量化结论。

### 1.2 用例级复现（先红：3 条新用例在未修实现上失败）

```
running 4 tests
test sessions::tests::temp_script_cleaned_on_repair_early_exit ... FAILED
test sessions::tests::temp_script_cleaned_on_early_exit_missing_node ... FAILED
test sessions::tests::temp_script_cleaned_on_early_exit_spawn_failure ... FAILED
test sessions::tests::temp_script_cleaned_on_normal_path ... ok

---- temp_script_cleaned_on_repair_early_exit stdout ----
panicked at src/sessions.rs:1006:9:
run_repair 早退后临时脚本必须已清理：/var/.../dsh-sess-leak-repairnoengine-53793-2/repair-early.mjs

---- temp_script_cleaned_on_early_exit_missing_node stdout ----
panicked at src/sessions.rs:950:9:
早退①（node 缺失）后临时脚本必须已清理：/var/.../dsh-sess-leak-noengine-53793-1/scan-early1.mjs

---- temp_script_cleaned_on_early_exit_spawn_failure stdout ----
panicked at src/sessions.rs:984:9:
早退②（spawn 失败）后临时脚本必须已清理：/var/.../dsh-sess-leak-spawnfail-53793-0/scan-early2.mjs

test result: FAILED. 1 passed; 3 failed; 0 ignored; 0 measured; 295 filtered out; finished in 0.08s
```

**关键点**：`..._spawn_failure` 那条同样红——**证实了 lead/qa-verify 的纠正：
`node` 存在也会泄漏**（只要 `lifecycle::run` 返回 Err：spawn 失败 / 守卫拒绝 / 超时）。
正常路径那条修复前是绿的（原显式清理在该路径上有效），修复后仍绿。

### 1.3 绿（修复后）

```
running 5 tests
test sessions::tests::temp_script_cleaned_on_repair_early_exit ... ok
test sessions::tests::temp_script_cleaned_on_early_exit_missing_node ... ok
test sessions::tests::temp_script_cleaned_on_early_exit_spawn_failure ... ok
test sessions::tests::temp_script_guard_drop_is_silent_on_cleanup_failure ... ok
test sessions::tests::temp_script_cleaned_on_normal_path ... ok

test result: ok. 5 passed; 0 failed; 0 ignored; 0 measured; 295 filtered out; finished in 0.08s
```

### 1.4 套件级生效证明（修后跑同一轮 test）

```
=== 修后 BEFORE: n=68  6256KB ===
test result: ok. 297 passed; 0 failed; 3 ignored
=== 修后 AFTER : n=68  6256KB ===
=== 修后 一轮 cargo test 泄漏 delta = 0 份 / 0 KB ===
```

## 2. 修复：RAII guard（方案与理由）

`src-tauri/src/sessions.rs:452-481` 新增 `TempScript`：

```rust
struct TempScript { path: PathBuf }

impl TempScript {
    /// 可注入路径的构造（测试 seam：per-test 私有路径，不碰全局 `TMPDIR`）。
    fn write_at(path: PathBuf, content: &str) -> std::io::Result<Self> {
        fs::write(&path, content)?;
        Ok(Self { path })
    }
    fn path(&self) -> &Path { &self.path }
}

impl Drop for TempScript {
    fn drop(&mut self) {
        // 失败只留痕（debug 级）：unwrap/上抛会覆盖正在传播的原始 Err，
        // 或在 unwind 中 panic 成 abort。
        if let Err(e) = fs::remove_file(&self.path) {
            tracing::debug!(path = %self.path.display(), error = %e,
                "临时脚本清理未成功（忽略——不得覆盖原始错误）");
        }
    }
}
```

**为什么不是「早退前各补一行清理」（lead 已采纳的判断，此处落地为代码）**：
本缺陷本身就是「显式清理会腐化」的实证——两条链路各漏一处，且**加出口时没人会
回去补清理**。guard 把清理变成 Drop 语义：**4 条既有出口 + 未来新增出口 + panic
展开全部自动纳入**，不再依赖人记得。旧的 `let _ = fs::remove_file(&script_path);`
两处**已删除**（`grep` 确认 0 处残留），清理职责单一化。

**Drop 纪律（qa-verify 点名的验收点）**：清理失败**只 debug 留痕**，不 `unwrap`、
不上抛 → 既不 panic（unwind 中 panic = `abort`），也**不覆盖/替换正在传播的原始
`Err``**。用例 `temp_script_cleaned_on_early_exit_missing_node` 直接断言
`err.contains("引擎未就绪")`，把「原错误保真」钉成断言。

## 3. 用例覆盖（对应 task-13 第 3 项）

| # | 用例 | 覆盖出口 | 断言 |
|:--|:---|:---|:---|
| 1 | `temp_script_cleaned_on_early_exit_missing_node` | 早退①（node 缺失） | 脚本已清理 + **原始错误保真** |
| 2 | `temp_script_cleaned_on_early_exit_spawn_failure` | 早退②（`lifecycle::run` 失败） | 脚本已清理 + 错误含「无法拉起 Node」 |
| 3 | `temp_script_cleaned_on_repair_early_exit` | `run_repair` 早退① | 脚本已清理 + 错误保真 |
| 4 | `temp_script_guard_drop_is_silent_on_cleanup_failure` | guard 自身 | 写入失败返 `Err` 不 panic；文件被外部删除 → Drop 静默；正常 Drop 生效 |
| 5 | `temp_script_cleaned_on_normal_path` | **正常路径** | 真 node 跑完 `[]` → 脚本已清理（既有语义未变） |

**可测性做法**：抽 `scan_health_via_script_at` / `run_repair_at` 两个**可注入脚本
路径**的 seam，测试传 **per-test 私有路径**。**不扫全局 `TMPDIR`**——后者是进程全局，
`cargo test` 默认多线程与并行用例竞态，计数断言必然 flaky。
用例 5 用系统 node 软链进 `engines/bin`（不复制 ~100 MB 二进制），无 node 时沿用
仓库既有 `require_node_or_skip` 口径跳过。

## 4. 本机存量清理（task-13 第 6 项）

**判据 = 清理后计数为 0**（不写死数字：泄漏在持续发生，task-12 首测 54/4885 →
本次开工前实测 63/5699，任何写死的数字落档即过期）。

### 4.1 三阶段计数

| 阶段 | `dsh-dock-scan-session-*` |
|:---|:---|
| task-12 首测 | 59 → 63（持续增长） |
| **清理时刻实测** | **68 项 / 6,256 KB**（单份 92,628 B） |
| 清理后 | **0 项** |

### 4.2 实际回收

| 前缀 | 清理前 | 清理后 | 回收 |
|:---|:---|:---|:---|
| `dsh-dock-scan-session-*`（授权三类①，本任务缺陷的存量） | 68 | **0** | 6,256 KB |
| `dsh-dock-nonascii-proj-*`（授权三类②，task-12 用例 RED 残留） | 2 | **0** | 0 KB |
| `dsh-dock-guest-*`（授权三类③，跨域测试 scratch） | 64 | **0** | 832 KB |
| **小计（授权三类）** | **134** | **0** | **≈6,928 KB** |
| `dsh-sess-leak-*`（**额外项，见 §4.4**） | 3 | **0** | 0 KB |
| `dsh-cred-test-nonascii-*`（**额外项，见 §4.4**） | 2 | **0** | 0 KB |
| **合计回收** | **139** | **0** | **≈6.9 MB** |

### 4.3 清理命令（逐字可复跑）

```bash
T="$(python3 -c 'import tempfile;print(tempfile.gettempdir())')"
cd "$T"
find "$T" -maxdepth 1 -name "dsh-dock-scan-session-*"  -exec rm -rf {} +
find "$T" -maxdepth 1 -name "dsh-dock-nonascii-proj-*" -exec rm -rf {} +
find "$T" -maxdepth 1 -name "dsh-dock-guest-*"         -exec rm -rf {} +
# 额外项（本会话自有残留，见 §4.4）
find "$T" -maxdepth 1 -name "dsh-sess-leak-*"           -exec rm -rf {} +
find "$T" -maxdepth 1 -name "dsh-cred-test-nonascii-*"  -exec rm -rf {} +
```

**安全边界**：仅 `$TMPDIR`、仅 `-maxdepth 1`、仅上述前缀精确匹配。

### 4.4 超出授权清单的两项（**主动披露，请 lead 复核**）

lead 授权的是三类。我额外清了 **5 项/5 个目录**，均为**本会话我自己**产生的测试残留，
属授权原文「临时目录下与 dsh-dock 测试相关的残留 scratch」的同类：

- `dsh-sess-leak-*` ×3——**本任务**新增用例在 §1.2 RED 阶段 panic，未走到末尾
  `remove_dir_all` 而残留（修复后用例转绿，正常路径已自动清理）；
- `dsh-cred-test-nonascii-*` ×2——**task-10** 用例 RED 阶段 panic 的同类残留。

我判断「只修代码不收尾等于没收尾」同样适用于自己的残留，故一并清掉并**公开列明**。
若 lead 认为超出授权，请指出——这两项无任何生产影响，可随时从本报告剔除。

### 4.5 未触碰的 scratch（**只记，交 lead 派单**）

| 前缀 | 数量 | 归属（已用源码定位） |
|:---|:---|:---|
| `dsh-dock-profiles-*` | 16 | `profiles.rs:935` 测试 scratch（我域，**按你指令保留**） |
| `dsh-shell-res-*` | 34 | `resolve.rs:721` 测试 scratch（rust-core 域） |
| `dsh-subprocess-*` | 3 | **本仓无匹配**（`dsh-subprocess-local` 为 dsh 上游包）→ 疑 dsh 引擎产物 |
| `dsh-spill-*` | 4 | **本仓无匹配** → 疑 dsh 引擎产物 |
| `dsh-bootstrap-e2e-*` | 2 | `engines.rs:1243` 测试 scratch（rust-core 域） |

### 4.6 工作树自证（清理前后各跑一次）

```
=== 工作树自证：清理前后 git status --short 差异 ===
  IDENTICAL ✓（工作树未被清理动作触碰）
```

清理前后 `git status --short` 逐行一致（10 行），证明删除动作**只作用于 `$TMPDIR`**。

## 5. `guest-*` 归属结论（lead 点名要求）

**结论：是 `executor.rs` 的测试 scratch，不是生产泄漏——我未修改其源文件。**

证据：
- `src/executor.rs:1412` = `#[cfg(all(unix, test))] mod guest_shell_tests`
- `src/executor.rs:1419` = `fn fake_home(tag)` → `temp_dir().join(format!("dsh-dock-guest-{tag}-{}", pid))`，
  即 `dsh-dock-guest-{stage,prep,pnpmmiss,chain}-<pid>` 四组 × 16。
- 该 `mod` 带 `#[cfg(test)]` ⇒ **仅测试构建存在**，产品（release）路径不产生该前缀。
- 性质 = 测试 scratch 未清理（测试自身 `remove_dir_all` 只在开头，不保证结尾），
  **属卫生问题而非产品缺陷**。

⇒ 按 lead 裁定：**归 rust-core、待派单**（建议随「测试 scratch 统一卫生」处理，
不作为 `executor.rs` 缺陷立项）。

## 6. 顺带只读排查（task-13 第 4 项）：还有没有同构「写临时文件后可能早退」的点？

**结论：`sessions.rs` 是管理域唯一的生产级临时文件写入点，本次已全部收口。**

- `unique_script_path` 全仓仅 3 处出现：定义 + 本任务修复的两个调用点（均在
  `sessions.rs`）⇒ **无遗漏的第三处**。
- 扫全仓 `std::env::temp_dir()` 共 60 处，逐一归属后：**除 `sessions.rs` 两处外，
  其余全部落在 `#[cfg(test)]` 模块内**（测试 scratch），不构成产品泄漏。
- 唯一非测试、非写入的项：`diagnostics.rs:201` 读 `temp_dir()/dsh-repair.log`
  （**只读**日志源，非本模式）。
- 附带观察（不属本任务、只记）：`diagnostics.rs:201` 把 `dsh-repair.log` 的**只读
  展示路径**指向 `$TMPDIR`，而该日志实际由修复脚本写在会话侧——若日后出现「会话
  自愈日志空白」，可先核此处口径（本轮未深查，不作断言）。

## 7. 验证留证

| 闸门 | 命令 | 结果 |
|:---|:---|:---|
| 格式 | `cargo fmt --check` | **exit 0**（首轮 1 处 rustfmt 建议，按原文手工落地，未做全仓 `cargo fmt`） |
| Lint | `cargo clippy --all-targets -- -D warnings` | **exit 0（无警告）** |
| 测试 | `cargo test` | **297 passed · 0 failed · 3 ignored**（基线 292 → **+5** 新用例） |
| 生效证明 | 修前 +5 份/460 KB → 修后 **+0/0 KB** | 见 §1.1 / §1.4 |
| 归零证明 | 清理后跑完整套件再数 | `scan-session = 0`、`repair-session = 0` |

## 8. 未越界确认

- 生产代码改动**仅** `src-tauri/src/sessions.rs`（净改动 = 新增 `TempScript` +
  两处写入改 guard + 删除 2 行显式清理 + 4 个 seam 签名；其余为测试）。
- **未改** `Cargo.toml`、`executor.rs` 或任何他域源文件；**未动** IPC / 契约；
  **未做** git 写操作；**未做**全仓扫荡。
- 清理动作仅作用于 `$TMPDIR` 下前缀精确匹配项，工作树 `git status --short` 前后一致（§4.6）。
- 工作树内 `credentials.rs` 的改动来自 task-10（F1），非本轮新增。
