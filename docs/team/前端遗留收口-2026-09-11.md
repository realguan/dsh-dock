# 前端遗留收口（task-27）

> 日期：2026-09-11 ｜ 执行：`frontend` ｜ 任务：task-27（fix，四项遗留合一）
> 证据强度：**实测**（逐项现状核对 + 门禁红→绿 + en 漏译基线回归 + sha256 校验复原）
> 基线：40 files / 299 passed → 交付后 **41 files / 306 passed**
> 范围：`components/boot/ErrorCard.tsx` · `App.tsx` · `components/layout/QuickDshSwitcher.tsx` · `types/events.ts` · `types/ipc.ts` · `stores/profilesStore.ts` · `pages/BootSelector.tsx` · `content/{zh-CN,en-US}.ts` · `__tests__/`（**未动 Rust**）

> ⚠️ **读前须知**：本文四项**均已交付**，但**存在一处已知未修的组件级逃逸路径**——
> `pages/BootSelector.tsx` 若被重新引入「判定读字典文案」，**现有门禁抓不住**（24/24 会全绿）。
> **该缺口尚未修**（lead 已裁定记入「测试门禁质量」任务，不在本轮补）。
> **不要把「task-27 完成」理解为「组件级也已设防」**。完整取证（三层注入实测 + 复现步骤）见
> [本文附录 A](#附录-a组件级门禁缺口变异测试取证)。

## 汇总（四项逐条）

| # | 项 | 核实结论 | 处置 |
| :-- | :--- | :--- | :--- |
| ① | `ErrorCard:66` 同串第三处 | **成立且更严重**：该文件共 **4 处可达 en 可见中文**（任务只列 1 处） | 修 4 处；`reselectHint` 复用，另 3 处新增/复用键 |
| ② | `EV.settingsChanged` 收敛 | **成立**：字面量硬编码 2 处 | 收敛到 `EV`，2 处改引；删 `App.tsx` 局部常量 |
| ③ | 死字段残留断言 | **成立，且原断言是在保护死代码** | 清理死代码 + **改写**断言（非删除） |
| ④ | 注释措辞 | **成立**：`default_cleared` 那条确易误读 | 只改注释，两语对称 |

---

## ① ErrorCard：同串第三处 + 全量扫描（4 处可达）

扫描口径同 task-25：剥离注释后逐行保留原行号扫 CJK，覆盖 JSX 文本 / 字符串字面量 /
模板串 / `title` / `aria-label`。共 6 处命中，逐条可达性：

| 行（改前） | 代码 | 可达性判据 | 分类 | 处理 |
| :--- | :--- | :--- | :--- | :--- |
| `:66` | `` setActionError(`${t.error.actionFailed}：${msg}（可返回重选）`) `` | **可达**：`run(id)` 的 `.catch` → `setActionError` → 渲染于 `:125-127`（`INVOKABLE` = retry/upgrade/upgrade_only，用户点按钮失败即命中） | **真 i18n 缺陷** | `` `${t.error.actionFailedDetail(msg)}${t.error.reselectHint}` `` |
| `:178` | `原始诊断日志 · 尾部 {payload.log.split("\n")...length} 行` | **可达**：`payload.log` 非空时渲染（`{payload.log && …}`） | **真 i18n 缺陷** | 新增 `t.error.rawLogSummary(n)` |
| `:188` | `title="复制日志"` | **可达**：日志折叠区内复制按钮 | **真 i18n 缺陷** | 新增 `t.error.copyLog` |
| `:198` | `<span>复制日志</span>` | 同上 | **真 i18n 缺陷** | 同上（同键两处消费） |
| `:193` | `<span>已复制</span>` | **可达**（`copied` 态） | **真 i18n 缺陷** | **复用既有 `t.boot.copied`** |
| `:75` | `logger.warn("[boot]", "复制诊断日志失败", …)` | 面向维护者、非 UI | **不应入字典** | 白名单化（同 task-25 口径） |

**注**：`ErrorCard` 由两处渲染——`pages/BootIndex.tsx:281`（`diag` 形态）与
`pages/BootSelector.tsx:390`（普通形态）——两条路径的用户都可能触发上述四处。

### ① 附：`:66` 暴露的第五处（全角冒号，任务未列）

原组合串 `` `${t.error.actionFailed}：${msg}（可返回重选）` `` 里的分隔符是**全角冒号 U+FF1A**
⇒ en 用户看到 `Action Failed：boom (…)`。修 `reselectHint` 并不能消掉它。

**处置**：按本仓既有惯例改为**组合键** `error.actionFailedDetail(msg)`
（同 `market.installFailed: (msg) => \`安装失败：${msg}\`` 的形态）——分隔符随语言定义：

| 键 | zh-CN | en-US |
| :--- | :--- | :--- |
| `error.actionFailedDetail(msg)` | `动作失败：${msg}` | `Action Failed: ${msg}` |

原独立标签 `error.actionFailed` 在改造后**唯一消费者消失**（实测全仓仅 `ErrorCard:66` 引用），
故**一并删除**，避免留死键（与 ③ 同一判据）。实测 en 求值：`"Action Failed: boom"`。

---

## ② `EV.settingsChanged` 收敛

**现状证据**（改前）：
- `App.tsx:37` `const EV_SETTINGS_CHANGED = "app:settings-changed"`（局部常量）；
- `components/layout/QuickDshSwitcher.tsx:42` `listen<ShellSettings>("app:settings-changed", …)`（裸字面量）；
- `types/events.ts:1` 头注释自述「事件名常量（**消灭魔法字符串**）」，`EV` 已有 5 键——
  **命名风格核实**：小驼峰键名（`bootStep`/`appUpdate`）+ `as const`；未存在同类键，故新增。

**改动**：
- `types/events.ts:19` 新增 `settingsChanged: "app:settings-changed"`（附 emit 点与载荷说明）；
- `types/events.ts:53-55` 新增 `export type SettingsChangedEvent = ShellSettings`
  ——**照该文件既有惯例**（`AppUpdateEvent = ClientUpdate` / `VersionsSnapshot = UpdateStatus`）；
- `App.tsx`：删局部常量，改 `listen<ShellSettings>(EV.settingsChanged, …)`（`:83`）；
- `QuickDshSwitcher.tsx`：改 `listen<ShellSettings>(EV.settingsChanged, …)`（`:43`）。

**类型契约影响**：**只加不改**——`EV` 原有 5 键与既有 payload 类型（`BootStepEvent` /
`BootErrorEvent` / `AppUpdateEvent` / `VersionsSnapshot`）**一字未动**；新增的是键与 payload 别名。
实测收敛后全前端仅 `types/events.ts:19` 一处定义该字面量（其余命中均为测试断言/注释）。

---

## ③ 死字段 `tag`：测试同步 + 死代码清理

### 现状证据（改前）：**原断言在保护死代码，而非防回流**

```js
// bootSelectorDefault.test.ts（改前）
expect(tagAssignments, "死字段 tag 疑似回流").toHaveLength(1)   // 允许恰好 1 处
expect(bootSelectorCode).toContain("tag: t.selector.customTag")  // ← 要求死代码必须存在
```

第二条**要求那个已无消费者的 `tag` 字段必须存在**——将来谁清理它，测试就红。
这是「测试防腐烂失败」的隐蔽反模式（看起来在保护质量）。

### 判据与处置

| 对象 | 判断 | 处置 |
| :--- | :--- | :--- |
| `BootSelector.tsx:140` `tag: t.selector.customTag` | task-23 只删了 `displayProfiles` 里的赋值，**回退 meta 这处漏删**；无任何消费者 | **删除** |
| 字典 `selector.customTag` | 删上句后**零引用**（实测 grep 仅命中字典定义与测试钉死） | **删除** |
| 字典 `selector.items[*].tag` | 同上（task-23 后已无判定用途，`meta.tag` 判定已解耦） | **删除**（含 `as Record<…>` 类型里的 `tag: string`） |
| `AppCopy` 类型影响 | `AppCopy = RecursiveString<typeof t>`（派生型），两语字典**同步删同名键**即类型安全；`en-US.ts` 亦为 `AppCopy` 类型，两处对称修改后 `tsc -b` 0 错 | **无残留影响**（实测） |

### 测试改写（**不是删断言**）

| 原断言 | 新断言 | 意图 |
| :--- | :--- | :--- |
| `toContain("tag: t.selector.customTag")` | `match(/^\s*tag:/gm)` **必须为空** + 不得含 `t.selector.customTag` | **防回流**（原为防腐烂失败） |
| — | 字典侧两语：`Object.keys(selector)` 不含 `customTag`、`Object.keys(items.web)` 不含 `tag` | 防只删一侧 |
| `not.toBe(String(items.web?.tag))` | `Object.keys(items.web)` 不含 `tag` | 由「碰巧不同」升级为「**根本不存在**」 |
| 变异靶子 = `items.web.tag` | 变异靶子 = `items.web.title`（**仍在渲染**的活键） | 保住**行为层演示**，不依赖已删键（否则断言 vacuous——永远通过） |
| — | 消费点计数上界由 3 收紧为 2 | 防重新引入 `meta.tag` 读取路径 |

保留的**真正有效的防回流断言**：无 `TEMPLATE`、无 `isDefault ? t.selector.defaultBadge` 三元、
判定函数体内无 `meta` / 无 `t.`、组件仍走 `{t.selector.defaultBadge}`。

### 另一处「测试钉住旧实现」的同步修正（由 ② 触发）

`i18nLocale.test.ts:213`（task-24 交付）原断言 `expect(appCode).toContain('"app:settings-changed"')`
——钉的是「魔法字符串就写在这里」。② 收敛后该断言必红（本轮实测红）。
按真实意图重写：`toContain("EV.settingsChanged")` + **反向** `not.toContain('"app:settings-changed"')`
（防魔法字符串回流）。**与 ③ 同类：断言应表达意图，不应钉住当时的实现形态。**

---

## ④ 注释措辞按 AGENTS §6 校正后口径统一

**现状证据**：两处均写「读取侧兜底 web」，与校正后口径（**None 由消费方兜底 `web`；失效值不消费**）
不完全一致；其中 `default_cleared` 那条**与实现不符**——实测 `commands/profile.rs:132-144`：

```rust
if settings.default_profile.as_deref() == Some(profile.as_str()) {
    settings.default_profile = None;      // ← 删除时置 None
    crate::settings::save(&data_dir, &settings)…;
    default_cleared = true;
}
```

⇒ 真实语义是「**删除时把引用置为 None**」，而非「保留失效值、读取时兜底」。

| 文件 | 改前 | 改后（**只动注释**） |
| :--- | :--- | :--- |
| `types/ipc.ts:205` | 「该 profile 是默认启动 profile，引用已清除（读取侧兜底 web）」 | 「删除时已把引用**置为 None**（指针到 `commands/profile.rs`）；之后按 AGENTS §6：**None 由消费方兜底 `web`**；**失效值不消费**」 |
| `stores/profilesStore.ts:12` | 「默认启动 profile；null = 未设置（读取侧兜底 web）」 | 「`null` = 未设置（settings 里为 None）。AGENTS §6：**None 由消费方兜底 `web`**；**失效值不消费**」 |

**未动**：`src-tauri/src/profiles.rs:692` 同款措辞——**Rust 不在本任务范围**，且 lead 已确认另派。

---

## 5. 新增键与删除键对照

| 键 | 类型 | zh-CN | en-US | 去向 |
| :--- | :--- | :--- | :--- | :--- |
| `error.rawLogSummary` | `(n) => string` | `原始诊断日志 · 尾部 ${n} 行` | `Raw diagnostic log · last ${n} lines` | 新增 |
| `error.copyLog` | string | 复制日志 | Copy log | 新增（与 `boot.copyDetail`「复制详情」语义不同，故未复用） |
| `error.actionFailedDetail` | `(msg) => string` | `动作失败：${msg}` | `Action Failed: ${msg}` | 新增（消全角冒号） |
| `error.actionFailed` | string | 动作失败 | Action Failed | **删除**（消费者归零） |
| `selector.customTag` | string | CUSTOM | CUSTOM | **删除**（消费者归零） |
| `selector.items[*].tag` | string | DEFAULT | DEFAULT | **删除**（消费者归零） |
| `boot.copied` | string | 已复制 | Copied | **复用**（未新建重复键） |
| `error.reselectHint` | string | （可返回重选） | （…） | **复用**（task-25 交付） |

---

## 6. 防复发门禁

| 测试 | 覆盖 |
| :--- | :--- |
| 新增 `__tests__/errorCardCopy.test.ts`（7 例） | ① UI 面不得出现裸中文（logger 行白名单化 + 防腐化断言 + 反向断言防过度剥离）② 新键两侧对称/类型/非空（含 `rawLogSummary` 求值）③ 新键 en 侧无 CJK ④ **复用而非新建重复键**（`boot.copied` 在、`error.copied` 不存在）⑤ `copyLog` 与 `boot.copyDetail` 语义分离 ⑥ 旧内联文案不回流 |
| `bootSelectorDefault.test.ts`（15 例，改写） | 见 ③ 表 |
| `i18nLocale.test.ts`（修正 1 例） | 见 ③ 末 |

**注释剥离**直接做对（`stripComments` + logger 行剔除），未重犯 `marketI18n.test.ts` 的已知问题。

### 6.1 复现先行：门禁对改前源码**实测红**

三项改动文件在本次任务前**无他人未提交改动**（`git diff` 逐条确认为本次），故可安全
`git show HEAD:` 还原该三文件：

```text
--- 新门禁对 HEAD（改前）实测 ---
× UI 面不得出现裸中文（logger 消息为显式例外）
× 例外白名单非空且有命中（防腐化）
× 复用既有键而非新建重复键（任务明确要求）
× 旧内联文案不得回流（本文件特征串）
× 死字段 `tag` 已彻底移除且**不得回流**
── Test Files 2 failed | Tests 5 failed | 17 passed (22)
current-sha256:  c79d1911f4533812093dda4281cf70cb09ae4f3acebaed75ed38705447242880
restored-sha256: c79d1911f4533812093dda4281cf70cb09ae4f3acebaed75ed38705447242880
RESTORE_OK (sha256 identical)
```

### 6.2 闸门（CI 同款口径）

```text
$ cd frontend && pnpm run typecheck && pnpm run lint && pnpm run test
$ tsc -b --pretty false
Found 0 warnings and 0 errors.
Finished in 13ms on 139 files with 96 rules using 8 threads.
 Test Files  41 passed (41)
      Tests  306 passed (306)
```

### 6.3 回归：en-US 漏译基线未退化

```text
en-US leaves: 652 | CJK: 1 -> console.localeZh="简体中文 (Chinese Simplified)"
```

仍恒为 task-18 白名单化的唯一例外（语言自名）；本轮增删键后未引入新漏译。
新增键实际求值：`error.actionFailedDetail("boom")` → `"Action Failed: boom"`（en）。

---

## 7. 与任务清单的偏差（说明）

1. **① 实为 4 处而非 1 处**（另加全角冒号）。任务只列 `:66`；其余 3 处在同一文件内、
   同一意图（en 用户可见中文），且**均可达**——修一处留三处会留下半成品。
2. **③ 的处置比「更新断言」更进一层**：不仅同步断言，还**清理了被断言的死代码**并
   **删除随之成为死键的字典条目**（`customTag` / `items[*].tag`），因 lead 已批准扩面。
3. **② 附带修正了一处 task-24 的失真断言**（`i18nLocale.test.ts:213` 钉住魔法字符串），
   否则 ② 无法落地——属 ② 的必要连带，非夹带。

## 8. 遗留（**只报告，未动**）

1. `src-tauri/src/profiles.rs:692` 同款「读取侧兜底 web」措辞——Rust 不在本任务域，lead 已另派。
2. `marketI18n.test.ts`（task-16 交付）的「旧文案不回流」断言**未剥离注释**——
   本轮已三次遇到同类假阳性（task-20/task-24/本任务），该文件是唯一未修的存量。
   修它需触碰已验收产物，建议单独立项统一。
3. 其它组件同类内联中文仍在（task-20 只读盘点约 124 处候选 / 19 文件），继续「一组件一任务」。

---

# 附录 A：组件级门禁缺口（变异测试取证）

> 记录人：frontend ｜ 日期：2026-09-11 ｜ 状态：**已实测确认，尚未修**（lead 裁定记入「测试门禁质量」任务）
> 缘起：lead 要求保留证据供 qa-verify 交叉核对，而该证据原只存在于会话消息中——
> **qa-verify 是独立 agent，读不到我的会话上下文**。按团队口径「频道留不住，落盘才存在」，
> 故完整迁入本文（lead 已授权；`docs/team/*.md` 属证据载体，不属被验证对象，不影响 qa-verify 快照判据）。

## A.1 被测门禁与基线

- 门禁文件：`frontend/src/__tests__/bootSelectorDefault.test.ts`（15 例）+ `bootSelectorCopy.test.ts`（9 例）
- 被防护对象：`frontend/src/pages/BootSelector.tsx` 的「默认工作台判定」不得读字典文案
- 基线 sha256（注入前；三次注入前后均一致）：

```
2f4c1a769068fdf3aac3ff3cb1c898b06e6cdf5d01bc38e73b9b0cee9ac681ac  frontend/src/pages/BootSelector.tsx
```

复原方法：`cp` 备份 → 注入 → 跑门禁 → `trap` 复原 → `shasum` 比对（每次均 RESTORE_OK）。

## A.2 三层强度（关键结论）

| # | 注入形态 | 位置 | 结果 | 结论 |
| :-- | :--- | :--- | :--- | :--- |
| A | 判定函数改回「读字典 tag」：`item?.tag === "DEFAULT"` 写进 `resolveIsDefault` | 纯函数级 | **9 / 15 failed** | ✅ 断言**能失败，非 vacuous**；task-27 对变异靶子的修法有效 |
| B | 组件内追加 `\|\| (meta as {tag?}).tag === "DEFAULT"` | 组件级 | **1 / 24 failed**（仅被「无大写字面量比较」子串断言抓住） | ⚠️ 仅形态匹配兜住 |
| C | 抽出常量 + 类型转换绕过子串：<br>`const DEFAULT_TAG_TOKEN = "DEFAULT"`<br>`… \|\| (meta as Record<string,string>).tag === DEFAULT_TAG_TOKEN` | 组件级 | **24 / 24 全绿 ⇒ 缺陷完全逃逸** | ❌ **真缺口** |

**A 组原始输出（断言确实能失败）**：

```text
× 未设置默认（null）⇒ 工厂默认工作台 web 标为默认
× 失效默认值 ⇒ 不消费，无任何卡片标为默认（AGENTS §6「失效值不消费」）
× 已设置默认 ⇒ 只有被设置的那一个标为默认
✓ 工厂默认常量是稳定标识而非文案（值为 profile 名）
× 空串默认值按「未设置」处理需谨慎：仅 null/undefined 触发兜底
× 把字典里**仍在渲染**的文案改掉后，判定结果**不变**
× 把两语字典的 defaultBadge 文案改掉，判定结果同样不变
× 切换 activeLocale（task-24 的语言初始化）不改变判定结果
× 判定函数不接收语言参数（签名即证据：无 locale 入参）
✓ 判据不再比较字典值
✓ 组件确实调用纯函数（不是留个导出摆设）
✓ 死字段 `tag` 已彻底移除且**不得回流**
✓ 默认徽章渲染仍走字典键（解耦未改渲染文案）
✓ 组件内已无任何字典 tag 消费点（结构上不可能再耦合）
× 同文件内无第三处「字典值当控制流」（大写字符串比较已清零）
── Tests 9 failed | 6 passed (15)
AssertionError: expected 'export function resolveIsDefault(name…' not to contain '.tag'
AssertionError: 仍有字面量控制流：=== "DEFAULT"
```

**C 组原始输出（完全逃逸）**：

```text
$ pnpm exec vitest run src/__tests__/bootSelectorDefault.test.ts src/__tests__/bootSelectorCopy.test.ts
      Tests  24 passed (24)
── 无任何失败 ⇒ 缺陷逃逸
```

## A.3 根因判据

现有两条防护均为**子串/形态匹配**：

- `expect(bootSelectorCode).not.toContain('meta.tag === "DEFAULT"')`
- `expect(comparisons).toEqual([])`，其中 `comparisons = bootSelectorCode.match(/=== *"[A-Z_]{2,}"/g)`

它们防的是「**照抄旧写法**」，而不是「**判定重新读字典**」这一**行为**。
换写法（抽常量、类型转换、改大小写、抽 helper）即可绕过。

## A.4 建议加固方向（**未实施**；lead 已裁定记入「测试门禁质量」任务）

1. **表达意图而非匹配形态**（本人在本轮原始提案，经 lead 裁定后**自行降级为「非根治」**）：
   把判定赋值断言为整行精确匹配（`const isDefault = resolveIsDefault(name, defaultProfile)`
   该行不得含追加子句）——可挡 A/B/C 及「追加 `||`」变体，
   但**挡不住抽 helper 函数或换赋值路径**，故**只是把绕过门槛提高，不是根治**。
2. **根治思路**（供「测试门禁质量」任务系统性回答「这类门禁该用什么机制才真正表达意图」）：
   - **AST 级断言**：断言判定路径上不得出现任何对 `t.selector.items` 的读取
     （从源码结构判定，而非子串匹配）；
   - **把判定收敛到不可绕过的单点**：判定只接受来自真实数据/常量的入参，
     组件层在**类型层面**拿不到字典值（如判定入参类型不含任何文案字段）。

## A.5 复现步骤（任何人可独立重跑）

```bash
cd <repo> && cp frontend/src/pages/BootSelector.tsx /tmp/bk.tsx
# 注入 C：把 `const isDefault = resolveIsDefault(name, defaultProfile)` 替换为
#   const DEFAULT_TAG_TOKEN = "DEFAULT"
#   const isDefault = resolveIsDefault(name, defaultProfile) || (meta as Record<string, string>).tag === DEFAULT_TAG_TOKEN
cd frontend && pnpm exec vitest run \
  src/__tests__/bootSelectorDefault.test.ts src/__tests__/bootSelectorCopy.test.ts
# 期望（当前行为）：Tests 24 passed  ← 逃逸（即本缺口）
cp /tmp/bk.tsx ../frontend/src/pages/BootSelector.tsx   # 复原
shasum -a 256 ../frontend/src/pages/BootSelector.tsx     # 应为 2f4c1a76…
```

## A.6 关联

- 同族问题（判据：**「门禁的失败条件是否真实可被制造」**）此前已现 3 例，加本缺口共 **4 例**，
  lead 已合并为「测试门禁质量」独立任务：
  ① `tag: …` 断言钉死死代码（本报告 §③）；
  ② 变异靶子 `items.web.tag` 删除后将 **vacuous**（永远通过，本报告 §③）；
  ③ `__tests__/i18nLocale.test.ts:213` 钉住字面量（本报告 §③ 末）；
  ④ 本附录：组件级形态匹配可被绕过。
- 本报告正文：§6.1 复现先行、§8 遗留。
