# plugins.rs 网络面豁免收窄：整文件 → 条目级（task-19）

> 性质 = **test/refactor（零行为变更）**｜执行 = `rust-mgmt`｜日期 = 2026-09-11
> 范围 = `src-tauri/src/network_gate.rs` **豁免表一条**（+ 本报告）
> 基线 = `cargo test` **312 passed / 0 failed / 3 ignored**（task-17 后）
> 落点 = `docs/team/`（`docs/team/README.md` §2.5）

## 0. 一句话

`plugins.rs` 的整文件豁免收窄为**条目级**（`fetch_runtime_snapshot`）；变异验证证明
**收窄前同一注入会被静默放过、收窄后被精确拦下**——防线变精确，现状行为零变化。

## 1. 改动（唯一一处，落 `network_gate.rs`）

```diff
     NetworkEntry {
         file: "src/plugins.rs",
-        item: None,
+        // 2026-09-11 收窄（task-19）：整文件 → 条目级。plugins.rs 生产段全模式
+        // grep 仅一处原语命中（`ureq::post`，见 `fetch_runtime_snapshot`），
+        // 整文件豁免会让**将来任何人往本文件加第二处触网都不被拦下**——
+        // 正是 boot.rs 那次漏登的成因形态（"这文件本来就能触网"的模糊感）。
+        item: Some("fetch_runtime_snapshot"),
         kind: Kind::Exempt,
         reason: "插件运行态回环只读查询（AGENTS §7 2026-08-29）：POST 127.0.0.1 /api/pluginInventory/list，2s",
         date: "2026-09-11",
     },
```

**`reason` / `date` 一字未动**（保持 AGENTS §7 登记口径）；`item` 匹配经
`item_ranges` 的 `fn {ident}` 前缀识别，`pub fn fetch_runtime_snapshot` 命中——
与 `boot.rs` 的 `authenticate_workbench_session` 同款条目级先例。

**独立复核「真的只有一处原语」**（未依赖 lead 转述，自跑）：

```
$ for pat in ureq reqwest hyper:: tonic:: UpdaterExt TcpStream TcpListener UdpSocket '"curl"' '"wget"'; do grep -n -- "$pat" src/plugins.rs; done
  命中 [ureq]:
    204:    let resp = ureq::post(&url)
  其余 9 条原语：无命中
```

该命中位于 `pub fn fetch_runtime_snapshot`（`plugins.rs:199-213`）之内。

## 2. 验证 a：收窄后现状仍绿（**无第二处原语**）

```
$ cargo test --lib network_gate
running 15 tests
test network_gate::tests::exemption_table_entries_are_well_formed ... ok
test network_gate::tests::cfg_predicate_evaluation_is_not_substring_matching ... ok
test network_gate::tests::comments_are_not_scanned ... ok
test network_gate::tests::item_scoped_exemption_does_not_leak_to_the_rest_of_the_file ... ok
test network_gate::tests::braces_in_literals_and_comments_do_not_break_item_extent ... ok
test network_gate::tests::production_code_after_mid_file_test_module_is_still_scanned ... ok
test network_gate::tests::only_test_only_cfg_items_are_excluded ... ok
test network_gate::tests::subprocess_network_binaries_are_reported ... ok
test network_gate::tests::unguarded_network_primitive_inside_test_module_is_not_reported ... ok
test network_gate::tests::exemptions_are_live ... ok
test network_gate::tests::registered_entries_have_no_in_process_primitives ... ok
test network_gate::tests::unguarded_network_primitive_in_synthetic_source_is_reported ... ok
test network_gate::tests::word_boundaries_prevent_identifier_false_positives ... ok
test network_gate::tests::crlf_sources_are_judged_identically ... ok
test network_gate::tests::production_network_primitives_are_registered ... ok

test result: ok. 15 passed; 0 failed; 0 ignored; 0 measured; 300 filtered out; finished in 0.10s
```

**15/15 全绿 ⇒ `plugins.rs` 生产段确实只有那一处原语**，本次收窄**未抓出第二处**，
故按 task-19 §4a **不触发「停下问 lead」的条件**。

## 3. 验证 b：收窄**真的生效**（三组变异，全部已复原）

> 变异均在临时状态下进行，完成后**按 sha256 逐字节复原**（见 §5）。
> 变异②③ 需要临时动 `plugins.rs`，属 task-19 明示授权的验证手段；**未留存任何改动**。

### 变异①（收窄后）在另一函数注入原语 → **报红**
在 `fn fs_err`（非豁免函数，`plugins.rs:135`）注入一行 `ureq::get(...)`：

```
thread 'network_gate::tests::production_network_primitives_are_registered' panicked
以下生产代码出现网络原语但未在 AGENTS §7 登记（唯一网络面 = updates.rs，ADR-0006）：
src/plugins.rs:136: [ureq] let _mutant = ureq::get("http://127.0.0.1/").call();
```

⇒ 条目级豁免**不外溢**：豁免范围外的触网被精确捕捉，且诊断给出文件:行与原语。

### 变异②（收窄前）同一次注入 → **被静默放过**（反证，本次收窄的核心价值）
把同一条目临时改回 `item: None`，同一注入下：

```
$ cargo test --lib network_gate::tests::production_network_primitives_are_registered
running 1 test
test network_gate::tests::production_network_primitives_are_registered ... ok
```

⇒ **这正是 lead 描述的漏洞形态被实机复现**：整文件豁免下，往 `plugins.rs` 里加第二处
触网**不会被任何闸门拦下**。收窄后（变异①）同一动作即刻报红——**防线由此变精确**。

### 变异③ 摘除该条目 → **恰好报出 1 处**（证明条目活着且精确）

```
src/plugins.rs:204: [ureq] let resp = ureq::post(&url)
```

⇒ 该豁免**只挡这一处调用点**：摘掉即有 1 处命中（非 0 = 不是空条目；非多 = 无溢出）。
这正是 `exemptions_are_live` 的双向判据（`network_gate.rs:900-921`：摘掉必须报违规 ∧
带上必须干净）在**本条目**上的具体读数——该测试**收窄后仍绿**（见 §2 清单），
即条目级形态同样满足「豁免必须是活的」这条自检。

## 4. 全量闸门留证

| 闸门 | 命令 | 结果 |
|:---|:---|:---|
| 格式 | `cargo fmt --check` | **exit 0** |
| Lint（host） | `cargo clippy --all-targets -- -D warnings` | **exit 0**（warning/error 行数 = 0） |
| Lint（win-gnu） | `cargo clippy --target x86_64-pc-windows-gnu --all-targets -- -D warnings` | **exit 0**（warning/error 行数 = 0） |
| 测试 | `cargo test` | **312 passed · 0 failed · 3 ignored**（与基线逐字一致） |

已安装 target 确认：`aarch64-apple-darwin` / `x86_64-pc-windows-gnu` / `x86_64-unknown-linux-gnu`。
（win-gnu 一条为 task-19 明列要求；依 AGENTS §1「clippy 需逐目标各跑一次」。）

## 5. 完整性 / 未越界证明（sha256 逐字节）

| 文件 | 任务前 | 收窄后（最终） | 说明 |
|:---|:---|:---|:---|
| `src/network_gate.rs` | `6cac2d96…883d02` | `091c2fae…93cfb3` | 唯一差异 = §1 的条目收窄（见下反向验证） |
| `src/plugins.rs` | `bdcca0dd…5bf165` | `bdcca0dd…5bf165` | **逐字节未变** ✓ |

**「我的改动恰是且仅是预期那一处」的反向验证**：在临时副本上把收窄反向还原后，
sha256 回到 **`6cac2d96…883d02` = 任务前值**，diff 恰为 `item: None` →
`item: Some("fetch_runtime_snapshot")` + 说明注释：

```
172c172,176
<         item: None,
---
>         // 2026-09-11 收窄（task-19）：…（5 行注释）
>         item: Some("fetch_runtime_snapshot"),
```

其余未越界确认：
- `plugins.rs` **零改动**（`git diff --quiet` 通过）——本任务是让防线精确，不是改被防对象；
- 未改 `Cargo.toml` / IPC / 契约 / `lib.rs`；未引依赖；无 git 写操作；无扫荡式改动；
- `network_gate.rs` 当前为 rust-core 新建的**未跟踪文件**（`?? src-tauri/src/network_gate.rs`），
  故 `git diff` 不显示其变更——上述 sha256 对照即为其变更审计口径。

## 6. 结论与遗留

1. **收窄完成且已验证**：`plugins.rs` 整文件豁免 → 条目级 `fetch_runtime_snapshot`；
   **未发现第二处未登记触网**（故无需上报 §7 登记不全）。
2. **无行为变更**：312 passed 与基线逐字一致；收窄只影响**未来**新增触网的可拦截性。
3. **遗留（供 lead 决策，非本任务范围）**：豁免表其余整文件条目是否也该收窄？
   本轮未动。就 `updates.rs`（唯一网络面本体，整文件豁免是设计意图）、`updater.rs`
   （自更新驱动，单点）而言，整文件豁免尚属合理；但 `boot.rs`/`plugins.rs` 已证明
   **条目级形态在本闸门可用且测试支持**，若日后某文件出现「整文件豁免 + 新增调用点」
   的组合，建议一律按本任务范式收窄。
