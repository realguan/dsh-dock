# 交接文档：ADR-0010 收尾三任务（F 复核 / G 启动页重构 / 总收尾）

> 交接时间：2026-09-04。前序会话已完成 ADR-0010 引擎倒置主线 A–E 五个增量并提交，
> 本文档把剩余工作交给下一个 agent（前端重构 + 收尾），**自包含**——不需要前序
> 会话上下文。动工前先通读本文件 + `AGENTS.md`（共享宪法）+
> `docs/adr/0010-engine-inversion.md`（本主线 ADR）。

## 一、必须内化的约束（动任何文件前）

1. **AGENTS.md 是共享宪法**：写入边界（§11）、IPC 三处同步（§7）、前端三红线
   （§4.4：依赖白名单 / 前端运行时禁发新网络请求 / 跨窗只经事件广播）、测试纪律
   （§5：不引测试依赖、Vitest 只测纯逻辑不引 RTL/jsdom）。
2. **用户日志改造标准（逐字，任务 G 前端侧必须遵守）**：
   > 纯逻辑零侵入：严禁修改任何业务原有逻辑、函数签名、返回值及错误处理流向；
   > 禁止空洞日志；安全合规：绝不打印密钥、Token、密码及用户敏感隐私数据（PII）；
   > 禁止循环轰炸：严禁在紧密循环或高频事件监听内部打印日志。
   前端统一 `frontend/src/lib/logger.ts`（格式 `[模块名] 行为描述 { 上下文参数 }`，
   debug 仅 dev 构建；**禁止直呼 console.***，例外已收口完毕）。
3. **永不入库**：`node-map/node-map-private.key`、`src-tauri/resources/pnpm/`、
   `src-tauri/resources/dsh-snapshot/`、Apple 签名材料（p12/API key 只走 gh secrets，
   绝不进聊天与代码）。仓库根目录未跟踪文件 `dsh-ark-plugin-install.sh` **不是本次
   主线产物，勿动勿提交**，去留由维护者定。
4. **提交规范**：`type(scope): 中文标题`，正文分点说明，带 `动到：` 行 +
   `AI-Assisted: ZCode（起草；人工逐行复核 + 全量闸门）`。收尾三件事（AGENTS §8.5）：
   测试绿 → 人肉读 diff → 落档 broadcasts。

## 二、当前仓库状态（2026-09-04 16:52）

- 本地 master 未推送提交（`git log origin/master..HEAD`）：
  | commit | 内容 |
  |:---|:---|
  | `a6b12a7` | 前端工具链 Native 化：**pnpm 12 + TypeScript 7 + Oxlint**（维护者侧完成，broadcasts 已落档） |
  | `5359bff` | **任务 F 已完成**：统一日志封装 lib/logger + console.* 收口 |
  | `bd2b2e3` | E：升级呈现（UpdateBanner + settings.dismissedUpdate） |
  | `e106830` | D：WSL 客体 glibc pnpm 投递 + 客体引擎链 |
  | `0dae87c` | C：探测层退役（−2799 行，引擎档唯一来源） |
  | `332196c` | B：更新检查/升级链改引擎来源 |
  | `f19af4e` | A：创建链+修复链切引擎档 |
- 测试基线：Rust 148 passed；前端 15 文件 100 passed。
- 工作树干净；另有未跟踪 `dsh-ark-plugin-install.sh`（见上，勿动）。
- **闸门命令（工具链已切换，旧 npm 命令作废）**：
  ```bash
  # Rust 侧（仓库根）
  cd src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test
  # 前端侧
  cd frontend && pnpm install --frozen-lockfile && pnpm run typecheck && pnpm run lint && pnpm run test
  ```

## 三、任务 F —— 已完成，交接方仅做复核

`5359bff` 已落地：`lib/logger.ts`（级别路由 / debug 门控 / 空上下文省略）+ 存量
console.* 两处收口 + logger.test.ts 三例 + AGENTS §4.3 登记一行。Rust 侧
println!/eprintln!/dbg! 零残留（可跑
`grep -rn "println!\|eprintln!\|dbg!" src-tauri/src/` 复核），结构化字段与
error/warn/info/debug 分级已随 C/D/E 增量落地，**无需再做 Rust 扫荡**。
复核若有遗漏点，按日志标准补即可，勿大规模重构。

## 四、任务 G —— 启动页前端重构（本轮主体）

**第一步必须调用 Skill 工具加载 frontend-design skill**（path:
`/Users/guan/.zcode/skills/frontend-design/SKILL.md`），按其设计方法论出一版
有明确美学主张的实现，再动代码。

### 语义背景（为什么可重构）

引擎倒置后环境准备核心逻辑已变（ADR-0010 台账「boot 节点」行）：
**step0 检查引擎 / step1 准备引擎 / step2 启动工作台**——step1 在首启会经历
在线引导（内置 pnpm 重铺 → `runtime set node` 字节下载进度 → `pnpm add -g dsh`），
之后启动离线零网络直通；旧的「探测系统 node/npm/dsh、版本过低 TooOld」叙事已死，
「首次使用需要下载 Node 运行时 · 仅此一次」这类旧文案语义已不准确（现在是
引擎三件引导，且之后全部走引擎目录）。前端启动页直接重构是用户明确授权。

### 动什么

- `frontend/src/pages/BootIndex.tsx`（主页面）与 `frontend/src/components/boot/*`
  （PulseBar / DownloadProgress / BootTimeline / ErrorCard / VersionChip）；
  `pages/BootMode.tsx`、`pages/BootSelector.tsx` 若涉及同样文案语义可一并看。
- 文案 `frontend/src/content/zh-CN.ts` + `en-US.ts`（**双语必须同步**，
  en-US 受 `AppCopy` 类型约束，漏键 typecheck 即红；boot 的 STEPS 与 headlines
  是两套独立文案，不得互相推断——文件头注释有警告）。

### 硬约束

1. **step 序号锚定后端**：时间线/步骤名不得自行重排——以 `src-tauri/src/lib.rs`
   与 `executor.rs` 中 `sink(<step>, ...)` 调用位点的实际序号为准（0=环境检测、
   1=引擎准备/引导、2=启动工作台；引导期字节进度经 `boot:progress` 事件，
   `boot:step`/`boot:error` 载荷形状见 `frontend/src/types/events.ts` 与
   `stores/bootStore.ts`）。改文案可以，改事件消费逻辑需先读 bootStore。
2. 依赖白名单（React / Radix / Zustand / Framer Motion / Lucide / 取数层）：
   **新增任何依赖（含字体包）先回写 AGENTS §4.4 白名单并广播，否则禁止**。
   字体若引入必须自托管到 `frontend/public/` 本地打包——**禁止运行时外链
   CDN**（触前端红线 2）。
3. 样式走 Tailwind v4 `@theme` token（bg-bg/text-ink/border-line 等既有 token
   见 `frontend/src/index.css`），禁硬编码 hex。
4. IPC 只经 `lib/tauri.ts` 的 `api`，Promise 必须 `.catch`；日志只经
   `lib/logger.ts`。
5. Vitest 只测纯逻辑（本次重构如抽纯函数则补测；不引 RTL/jsdom）。
6. 已挂载的 `UpdateBanner`（升级提示条，挂点在 header 下方 top-14 浮层容器）、
   `VersionChip`、控制中心按钮、WSL 切换按钮的**行为语义保留**，视觉可重设计；
   `data-tauri-drag-region` 拖拽区语义保留。
7. 完成后全量闸门（见 §二）+ 人肉读 diff + 提交 + broadcasts 落档。

## 五、总收尾 checklist（G 完成后）

1. **broadcasts.md 落档**（`docs/broadcasts.md`，条目插在条目区最上、新条目在上；
   格式照抄既有条目）：为 C（`0dae87c`）、D（`e106830`）、E（`bd2b2e3`）、
   F（`5359bff`）、G 各补一条「完成通知」——注意 C/E/F 已触宪法级（AGENTS §2
   resources/pnpm/ 永不入库、§6 例外册新增 `dismissedUpdate`、§7 boot 期
   `npm i -g pnpm` 条目标注退役、§4.3 日志规范行），条目里必须写明「触宪法级 +
   具体条款」，占用声明口径 = 维护者会话内指示（沿 P3-b 条目先例）。
2. **推送**：`git push origin master`（上述全部提交一次推齐；推送前再跑一遍
   双侧闸门确认绿）。
3. **给维护者的联调指引**（聊天里交付即可）：
   - `cargo tauri dev` 首启：终端（dev 双写 stdout）应依次看到 pnpm 重铺 /
     `runtime set node`（带字节进度）/ `pnpm add -g dsh` 阶段日志；启动页
     step1 显示引导进度。引擎落点 `<app_data>/engines/`。
   - **离线验证**：首启成功后断网重启 → 零网络直通启动（惰性版本闭包语义）。
   - 插件操作 / 创建 profile / 会话修复均走引擎 node/dsh，系统装没装 node 不再
     影响（可改名系统 node 验证）。
   - Windows 实机 WSL 模式：客体引擎链 = pnpm 投递（\wsl$ 主通道）→ 客体内
     runtime set → add -g；musl（Alpine）发行版出可行动错误。旧目录
     `~/.dsh-dock/node` 弃用不迁移。
   - 升级呈现：dsh 有新版时启动页顶部非阻断提示条；「忽略此版本」写入
     `settings.json` 的 `dismissedUpdate`，同版本不再弹；更新动作在菜单/托盘
     「关于」更新中心。
   - 诊断页现在展示引擎四件套（pnpm/node/dsh 版本与落点，source=engine）。
4. pnpm 升级清单（ADR-0010，若 G/收尾期间 bump 过 pnpm 版本则必过）：
   runtime set 可用、`pnpm add -g` 可用、dsh 内 `spawnSync("pnpm")` 可达、
   三平台 boot 冒烟——CI（tag 构建跑 build.yml）+ 实机。

## 六、遗留与已知边界（不阻塞收尾，但要知道）

- `dsh-ark-plugin-install.sh`（未跟踪）：等维护者裁定。
- pnpm 二进制安装包内仍为明文存储（~32MB/平台）；「压缩存储 + 首启解压」已在
  ADR-0010 §6 复审条件裁定但**未实现**，属后续工作。
- macOS 公证 exec 级行为需真实发布产物复核（挂发版验收清单，ADR-0010 P0③）。
- `resolve_toolchain` 引擎半就绪时直接给可行动错误（不再回退系统探测）——若联调
  发现「装好引擎前点插件操作」体验问题，属预期行为，文案可议但语义不改。
