# DSH Dock 版本发布与升级日志 (Release Notes)

> 本文档记录 DSH Dock 各版本的核心演进、功能特性、交互优化与缺陷修复历史。

---

## [v1.2.1] - 2026-09-11

### 🌟 核心亮点 (Highlights)
- **Windows 启动失败不再「说错原因」**：本地模式因系统权限装不上引擎时，过去一律报「registry 均不可达」把用户引向重试网络；现在如实告知是**权限不足**，并直接给出「改用 WSL 模式打开」的可用出路。
- **启动与更新体验的三处卡顿修复**：启动页「查看启动详情」能收起了；客户端更新点「下载」后立即有反馈且进度条实时推进；切换运行环境后不再残留上一次的错误卡片。

### ✨ 新增功能 (Features)
- **「下次默认打开方式」有了窗口内入口**：此前只能在启动页通过隐藏参数、或在 Windows 托盘菜单里设置，应用界面内无处可改。现可在「系统控制台 → 偏好与守护」中选择本机默认以**本地**还是 **WSL** 模式启动。

### 🛠️ 体验与交互优化 (Improvements)
- **启动页「查看启动详情」可收起**：点开详情后页面上此前没有任何收起入口（Windows 本地模式与 WSL 模式都会遇到），只能重启应用；现在用户自己展开的就能自己收起，错误信息仍独立显示、不会被藏起。
- **客户端更新反馈即时化**：点「下载」后不再有一段无反馈的等待期，立即进入可见状态；下载进度条按实际进度实时推进（此前只显示首个采样点后即冻结）。
- **切换运行环境后清理过期错误**：从本地模式切到 WSL（或反向）后，迁移成功画面不再挂着上一轮的失败原因——错误描述的必须是当前这个世界的状态。

### 🐛 缺陷修复 (Bug Fixes)
- **Windows 本地模式引擎引导失败（权限类）**：根因是 pnpm 12 的全局安装需要创建符号链接，而普通账户（未开启开发者模式、非管理员）没有该权限。修复拆开了「网络不可达」与「权限不足」两类失败，**权限类判据优先**，并给出可行动建议。
- **关于页与健康大盘版本结论矛盾**：WSL 模式下健康大盘显示 dsh 已就绪，关于页却显示「未检出」。现版本探测按当前运行环境择源（WSL 模式下读客体内的实际版本），两个入口结论一致。
- **点「检查更新」会把关于页刷回「未检出」**：同一矛盾的第二条路径，已一并修复。
- **错误卡「改用 WSL 打开」是空按钮**：该按钮此前未接入实际动作，点了没有反应；现已接通并补上文案。
- **更新进度事件被整条丢弃**：Rust 侧新增的「准备中」状态未被前端状态表接受，导致下载进度从第一条起被全部丢弃、界面停在「检查中」。已补齐并**新增跨层契约机器闸门**，两侧任一方单独改动都会构建失败。

### 🔒 架构、安全与稳定性 (Security & Stability)
- **启动轮次状态收口**：把「开启新一轮启动」与「撤销上一轮可见状态」合并为**同一个动作**。此前两者分开、靠 7 处调用点各自记得配对而实际只有 3 处做到，导致崩溃自动拉起与 profile 选择器路径会把上一轮的错误留在缓存里，并随任意页面重载重新显示。
- **跨层契约闸门（新纪律）**：「事件生产方与消费方的测试各自通过」不等于契约成立。本轮更新链路出现两次「两侧单测全绿、跨界即断」，现以机器闸门强制校验「Rust 发射序 × 前端消费表」，并立为团队纪律 M12。
- **测试夹具密封化**：一处依赖宿主 `PATH` 的测试夹具在 CI 上被 runner 自带工具顶替而失败（本机因巧合为绿，属结构性覆盖盲区）。现将夹具改为密封位置并加自证断言；该根因在 2026-08-26 已出现过一次，已登记 roadmap §4.17 防复发。

### ⚠️ 已知限制
- **Windows 普通账户下的「本地模式」仍无法安装引擎**（属 pnpm 12 上游缺陷，需符号链接特权）。本次修复的是**诊断准确性与出路引导**，未改变安装能力。可选出路：改用 **WSL 模式**启动（推荐），或以管理员身份、或开启 Windows 开发者模式后重试。
- 本批修复经三平台 CI 全绿验证；其中涉及 Windows / WSL 运行时的行为为**机制级修复 + 单元与契约级验证**，**真机复验待排期**。

---

## [v1.2.0] - 2026-09-11

### 🌟 核心亮点 (Highlights)
- **WSL 模式下控制中心真正管到「客体世界」**：以前在 WSL 模式打开控制中心，操作的是宿主环境而 dsh 与引擎都在 Linux 客体里，于是插件装卸、profile 创建必然失败且重试无效。现在插件、Profile、会话、凭据、诊断全部按当前运行模式自动择源，在 WSL 模式下也能正常用了。
- **新增 macOS Intel (x86_64) 原生安装包**：此前只提供 Apple Silicon 版，Intel Mac 需自行编译；现在两种架构都有官方 `.dmg`，各自内置对应架构的引擎、原生性能不打折。

### ✨ 新增功能 (Features)
- **WSL 客体管理面（ADR-0016）**：控制中心在 WSL 模式下的读侧与写侧全面下沉到客体——插件行表与开关、插件更新检查、插件配置复制、Profile 的创建/复制/重命名/删除、会话扫描与维护、凭据读写、系统诊断，均在客体内完成；并新增客体侧构建策略写入，解决客体里 `pnpm add` 带构建脚本插件必失败的问题。
- **世界择源与同源护栏**：启动时记录本次实际选中的 WSL 发行版，管理面与运行中会话始终保持同一个世界；WSL 模式下通过 `\\wsl$` UNC 路径可直接打开客体工作区与会话目录。

### 🛠️ 体验与交互优化 (Improvements)
- **跨窗口语言设置即时生效**：修复切换界面语言后部分窗口仍显示旧语言/残留中文的问题，首帧即按偏好渲染。
- **文案收口（中英对称）**：市场、启动页、错误卡片、PreferencesPane、Profile 详情等多处硬编码中文与漏译修正；英文界面下不再出现中文。
- **不再出现「数字对不上」的文案**：清掉写死的插件数量（`2700+`）与「240+ 预置服务」等会随版本漂移的表述，改为与真实数据一致或不依赖具体数字。

### 🐛 缺陷修复 (Bug Fixes)
- **崩溃修复（两类，release 下直接闪退）**：凭据列表在值含中文/emoji 等非 ASCII 字符时崩溃；会话目录名含中文时崩溃（目录名可由用户手工创建/改名）。两处均改为按字符处理，并补齐回归测试。
- **磁盘临时文件泄漏**：会话修复链路有 4 条提前退出路径未清理临时脚本（约 90KB/次），改为作用域结束自动清理，并回收存量残留（约 6.9MB）。
- **插件/MCP 配置写入会丢掉用户手写注释**：写入 `cordis.patch.yml` 时会整表重序列化，导致人工注释丢失且不可逆；现统一为保真写入——未改动的条目逐字节原样保留，写入前自动备份、写入走原子替换。宿主与 WSL 客体两条路径现已共用同一套实现，行为一致。
- **WSL 模式停止/就绪信号静默失效**：宿主与客体两处路径靠注释维系一致，改一处即导致停止操作无效且测试仍绿；现改为单一来源并由测试钉住。
- **会话扫描兼容性**：修复在 BSD `stat`（macOS）与 Windows 路径反斜杠下的脚本兼容问题，三平台 CI 单测恢复全绿。

### 🔒 架构、安全与稳定性 (Security & Stability)
- **「唯一网络面」从纪律升级为机器闸门**：新增源码扫描闸门，`updates.rs` 之外出现网络原语即构建失败，需显式登记豁免；本轮同时补登了两处此前漏登记的网络用途（工作台 Token 环回兑换、客户端自更新）。
- **子进程生命周期收口加强**：新增依赖纪律与 spawn 收敛闸门，禁止裸 `Command::spawn`，确保退出/崩溃/强制结束都不留孤儿进程；`taskkill` 参数收敛为单一来源。
- **发布矩阵扩展**：CI 新增 macOS Intel 构建通道（交叉构建），更新清单新增 `darwin-x86_64` 目标，Intel 用户可获取原生包与自动更新。

---

## [v1.1.1] - 2026-09-11

### 🌟 核心亮点 (Highlights)
- **「会话点不开」的老毛病根治**：以前某个工作台会话会突然打不开、提示被占用，只能删掉会话再重启应用——现在应用自己会收干净残留进程，重启即可恢复，不必再牺牲会话数据。
- **Windows 首次开箱即用**：修掉了普通账户（未开「开发者模式」）下引擎组件根本装不上的问题，此前它会连带导致 DSH 未检出、健康大盘全空、插件装不上。

### 🐛 缺陷修复 (Bug Fixes)
- **会话被占用 / 打不开**：工作台会话偶发「已被活动的写入句柄占用」而无法进入，且删除会话 + 重启是唯一出路。根因不是会话本身，而是应用被强制结束（崩溃、任务管理器强杀、开发期重编译）时留下的残留进程一直占着该会话的写入权。现在应用在任何结束方式下都会回收自己的子进程，并在下次启动时清理历史残留——**重启即可恢复，不再需要删会话**。
- **Windows 引擎组件装不上（普通账户）**：未开启「开发者模式」的普通账户下，首次启动会卡在「引擎引导失败」并导致 DSH 未检出、健康大盘三张卡全空、插件安装报「引擎未就绪」——这一批现象是同一个根因。现已改用不需要符号链接的组件布局，普通账户可直接完成首次准备。
- **重启 / 切换后短暂白屏报错**：重启或切换工作台期间，界面会短暂出现「localhost 拒绝连接」再自行恢复。原因是正式版被错误地指向了仅开发环境存在的地址，现已修正。
- **从悬浮胶囊打不开控制中心**：在工作台点顶部悬浮胶囊打开「控制中心」时，窗口会白屏、卡死且关不掉（从系统托盘进入则正常）。现已修复，两个入口行为一致。
- **强杀应用后再次启动卡住**：强制结束应用后再启动，会永久卡在「引擎引导失败：落位 …pnpm.exe」，只能重装。现已可自愈，并在失败时给出完整原因而不是仅一个路径。
- **关于页版本号不实**：Node 运行时未安装时，关于页仍显示一个「应用托管」的版本号（与健康大盘的「未检出」自相矛盾）——那个版本从未被安装过。现在如实显示「未安装（计划 vX）」。
- **启动详情点开后回不去**：在 WSL 模式下点「查看启动详情」后，页面上没有任何收起入口，只能重启应用。现已补齐。
- **下载提示版本号重复**：WSL 引导提示显示为 `node vv24.18.0`（多一个 v）。已修正。

### 🔒 架构、安全与稳定性 (Security & Stability)
- **子进程生命周期（ADR-0015）**：应用对所有自己拉起的子进程建立登记与守护，无论正常退出、崩溃还是被强制结束都会被回收；下次启动还会清理此前遗留的孤儿进程。此前这类残留会一直占用工作台会话的写入权。
- **开发 / 测试与正式版彻底隔离**：开发构建与测试使用各自独立的数据目录，不会再干扰你正在使用的正式版及其工作台会话。
- **机器闸门扩容**：新增「子进程必须经受守护的通道启动」与「启动时必须执行残留清理」两道源码闸门，并扩大了关键修复的变异测试覆盖——机制被改坏即测试变红。

---

## [v1.1.0] - 2026-09-10

### 🌟 核心亮点 (Highlights)
- **重启与切换不再「断片」**：从点击重启到新工作台载入，全程由一条连续 loading 贯穿——控制中心有阶段导轨与跨窗口不归零的计时，主窗口沿用日常启动屏承接，工作台页面的文档替换空档由幕布覆盖；交接失败在你所在的窗口即可见。
- **界面配色系统性收口**：同一语义不再有两三个颜色——状态四族按等明度重建，「进行中」不再伪装成警告、「失败」不再显示为琥珀；文字三级灰阶、卡片层次与深色终端面同步重建。

### ✨ 新增功能 (Features)
- **重启 / 切换交接带**：控制中心吸顶呈现四段进度（停旧会话 → 启动新会话 → 等待就绪 → 进入工作台）与连续计时；交接真正结束前，「运行中」徽标不再提前点亮。
- **交接失败可见**：启动失败在控制中心与主窗口同时给出失败态与可见出口，不再只沉淀在后台日志里。

### 🛠️ 体验与交互优化 (Improvements)
- **设计 token 收口**：状态四族（成功 / 进行中 / 需注意 / 失败）与分类档按 OKLCH 等明度重建、两两可辨识；全仓 200 处绕过语义 token 的原生调色板归位，同一件事不再有 2–3 个色。
- **文字层次**：主 / 次 / 三级灰阶重建为真正可辨的三级（相邻可辨识度提升约 5 倍）；卡片、凹陷填充与分隔线的分层同步拉开，卡片不再「浮不起来」。
- **卡片与浮层**：投影改为带冷色偏的双层高度语义（浅色面上不再发脏），圆角按元素角色收敛为控制件 / 面 / 胶囊三档。
- **深色终端面**：运行日志、凭据、引擎设置原文与诊断日志统一为同一套深色语义，日志级别配色与之同源。
- **启动屏**：重启沿用日常启动屏本身（同一屏、同一构图，仅标题与阶段副题随交接变化），不再叠加一层与启动流程重复的进度界面。
- **冷启动**：首帧底色、样式 token 与原生窗口底色三处对齐，消除启动瞬间的闪色。

### 🐛 缺陷修复 (Bug Fixes)
- **子进程逃逸**：并发启动不再覆盖会话槽丢弃旧 dsh；退出与启动的竞态不再残留孤儿；Windows 停止改为整进程树收口——旧实现只结束 `cmd.exe` 壳层，pnpm shim 背后的 node 仍在后台运行。
- **失败态误用警告色**：启动失败卡、客户端更新失败、版本列表加载失败、插件导入失败、确认框错误等全线归位为「失败」语义色；创建对话框里「进行中」与「失败」两个分支曾渲染成同一颜色，现已区分。
- **同一状态两个颜色**：工作台「运行中」在 Profile 列表与会话维护曾分别用绿与蓝，现统一为「进行中」语义；「NPM 官方包」来源徽标在三处曾分别用危险红与成功绿，现统一为中性。
- **删除对话框深链**：`?dialog=delete` 未携带目标时不再臆造一个硬编码的工作台名，改为不弹窗。

### 🔒 架构、安全与稳定性 (Security & Stability)
- **启动代际闸门**：以启动代际令牌取代晚读的探测序号，在探测后 / 派发前 / 落槽前 / 导航前逐点校验，两次快速切换不再双双放行（ADR-0014）。
- **进程生命周期**：会话槽改为「先收旧、再落新」；应用退出先置关闭标志再收会话，退出竞态不留孤儿。
- **设计 token 机器闸门**：四道闸门（对比度与可辨识度、调色板收口、形状与高度语义、跨文档镜像同步）把「改色即验算」变成 CI 约束，绕过 token 或写裸色值即测试红。
- **契约**：`get_boot_status` 新增 `intent` 字段（向后兼容）；`switch_profile` 返回值由空改为交接意图快照。

---

## [v1.0.0] - 2026-09-10

### 🌟 核心亮点 (Highlights)
- **DSH Dock 1.0 正式发布**：启动体验双模重塑——日常启动秒进、多工作台走全新「启动台」；会话自愈新增世代分叉修复；三平台应用图标与文档焕新为专属品牌形象。

### ✨ 新增功能 (Features)
- **工作台启动台（Launchpad）**：多工作台时以卡片网格呈现全部工作台及其运行态、默认标签与插件数，支持数字键 `1-9` 快捷直达与「记住我的选择」（下次启动直接进入）；单工作台场景走秒启 Splash，不再打断启动。
- **DSH 版本选择器**：从 Registry 拉取全版本列表，按稳定 / 候选 / 预览（Stable / RC / Alpha）通道标注并显示发布日期，可安装任意历史版本或回退到旧版；预览版需显式选择才会升级，日常「有新版」判定仍只认稳定 / 候选。
- **插件市场安装队列与下载管理**：异步安装不阻塞界面，实时呈现排队中 / 安装中 / 已安装 / 失败状态，配可收起的下载管理面板与安装抛物线动画。
- **补丁包启用 / 禁用开关**：依据 dump 段落归属映射生成贡献行，可单独停用某个补丁包而无需卸载。
- **破坏性操作统一确认与覆写前备份**：所有覆写类操作（凭据、引擎设置等）先落 `.bak-<时间戳>` 备份，并在执行前给出统一确认。

### 🛠️ 体验与交互优化 (Improvements)
- **启动链路**：新增日常秒启 Splash；工作台选择页撤除通栏顶栏；引擎已就绪时启动短路，去掉冗余等待。
- **会话维护**：`unknown` 状态独立呈现（不再与「需自愈」混淆）；会话行信息去重，字号刻度与断点对齐，页头吸顶。
- **可访问性**：键盘与读屏可达性补齐（表单标签、标题层级、toast live region、控件可读名称）；表单对比度达标并清理 dark 变体残留。
- **界面一致性**：图标与动词统一、长串截断补齐 `title` 提示、弹窗横向截断根治（grid 轨道被 nowrap 长串顶开的根因）。
- **品牌形象**：3D 鲸鱼娘 Whale-chan 成为客户端图标与页内徽章的统一形象（透明切图 + Apple HIG 圆角方框，四周透明度强门禁）。
- **文档**：README 按模块重构并配 14 张实拍界面图，新增更新日志入口与首次启动联网说明。

### 🐛 缺陷修复 (Bug Fixes)
- **会话自愈**：修复成功后失效 dsh 投影缓存（stale blank 投影导致侧栏隐藏）；迁移器不再把无法归类的会话标记为 `unknown`，消除「需自愈」误报。
- **插件市场**：修复非 npm 来源（GitHub / tarball）安装必败；修正 git 来源插件的已装状态判定（补声明 spec 连接键）。
- **外挂插件清单**：DSH 官方桌面运行时内置的 `desktop-packages` 不再被当作第三方外挂插件展示。
- **导航**：壳页面回流改用原生 `navigate`（替代注入 `location.assign`），消除启动与切换运行模式时的白屏与竞态。
- **运行日志**：`plugin-op.log` 改为追加式 + 轮转，失败历史不再被单次操作覆盖。

### 🔒 架构、安全与稳定性 (Security & Stability)
- **启动失败类型化**：boot 路径引入 `BootFailure` 枚举，子串分类降级为 `from_legacy_detail` 兜底（ADR-0012）。
- **插件职责边界立宪**：跨 profile 归插件中心、单 profile 归详情；安装来源三形态白名单（npm / GitHub / tarball），更新检查保持严格 npm 判别（ADR-0011）。
- **构建脚本默认批准**：profile 级 `dangerouslyAllowAllBuilds`，逐包裁决链退役（ADR-0013）。
- **模块收敛**：拆分 `boot` / `ui` / `commands` 独立模块，注入脚本迁出 Rust 原始字符串；剪贴板写入收敛为唯一入口。
- **契约闸门**：IPC 契约「形状」跨语言闸门（`ipc-shapes.json` 与前端类型一致性测试）；网络面 HTTP seam 注入，镜像链可离线覆盖。
- **发版纪律**：落盘发版日志强契约（`## [vX.Y.Z] - YYYY-MM-DD`）与 CI 提取脚本严格模式，杜绝裸打 tag。

---

## [v0.9.6] - 2026-09-08

### 🌟 核心亮点 (Highlights)
- **pnpm 12 构建审批门产品化与运行态会话误判自愈**：插件安装遭遇依赖构建受阻时提供交互式白名单逐包审批；修复已结束会话状态识别反转；治理 MCP 循环刷新与全站重载按钮语义。

### ✨ 新增功能 (Features)
- **构建脚本审批交互链路**：当 pnpm 12 触发 `ERR_PNPM_IGNORED_BUILDS` 时，前端主动弹出逐包裁决对话框，支持受控写入 `pnpm-workspace.yaml` 的 `allowBuilds` 白名单并自动重试。

### 🛠️ 体验与交互优化 (Improvements)
- **会话状态感知纠正**：修复 `engine_session_alive` 逻辑反转导致的已结束会话误判「进行中」问题，增订 `endState` 降噪子句。
- **MCP 面板异常治理**：根治内联引用与 effect 造成的死循环刷新，改用面板内错误态并保留安全重试。
- **全站刷新与操作语义化**：移除多余的刷新控件，规范日志与凭据重新拉取的语义标签和图标。
- **启动选择页视觉降噪**：撤除 selector 页面冗余顶栏，收紧工作台悬浮胶囊挂载条件，消除本地页面误挂问题。

---

## [v0.9.5] - 2026-09-07

### 🌟 核心亮点 (Highlights)
- **启动引擎进度阶段化与生命周期守护加固**：细化引擎初始化与流式下载进度反馈，彻底解决长时等待卡顿假象；完善 SIGTERM/SIGINT 优雅退出收孤儿与跨平台兼容。

### ✨ 新增功能 (Features)
- **引擎启动流式进度**：引擎初始化、下载与依赖检查链路支持分阶段细粒度进度通知，启动过程透明直观。

### 🛠️ 体验与交互优化 (Improvements)
- **优雅退出与孤儿清理**：响应系统关闭与中断信号，彻底回收后台子进程，杜绝进程孤儿驻留。
- **Windows 跨平台体验加固**：完善 guest engine 路径定位，修复 Windows 平台文件占用重试与单测兼容性。

---

## [v0.9.4] - 2026-09-01

### 🌟 核心亮点 (Highlights)
- **Windows 与 WSL 跨平台启动体验全面加固**：落地 WSL 客体内 Node.js 全自动补齐链路（ADR-0004 §7）；优化首次准备环境等待反馈与平滑过渡；实现模式切换双向感知与控制中心 Win32 WebView2 防白屏。

### ✨ 新增功能 (Features)
- **WSL 客体内自动补齐 Node.js 运行时**：当 WSL 发行版缺少 Node 时，通过 npmmirror/nodejs.org 镜像链在客体内全自动拉取官方 Linux-x64 tarball 并解压至 `~/.dsh-dock/node`，串联 `node -> dsh` 自动化安装与校验。
- **模式切换双向感知**：启动页模式切换按钮动态识别当前环境，在 WSL 模式下支持一键切换回「在本机中打开」，Local 模式下支持「在 WSL 中打开」，支持双语国际化文案。

### 🛠️ 体验与交互优化 (Improvements)
- **环境准备阶段顶栏精简与拖拽优化**：启动准备期间精简顶栏，隐藏未就绪的控制中心入口与品牌字样，保留透明可拖拽区域与版本信息，就绪后恢复控制中心入口。
- **首次下载准备反馈优化**：下载完成后进度条平滑过渡，在宿主解析、依赖准备与包管理器检查阶段注入细致可行动的中文提示，缓解长时等待焦虑。

### 🐛 缺陷修复 (Bug Fixes)
- **启动进度卡顿**：修复 Local 模式下 Node.js 运行时下载到 100% 后进度卡片长时间驻留的问题。
- **控制中心窗口防护**：修复 Windows 平台从胶囊唤起控制中心窗口在 WebView2 异步加载阶段出现白屏闪烁的问题。
- **WSL 工具链探测**：修复检测到 WSL 缺少 Node 时直接中断报错、未触发自动补齐的问题。

---

## [v0.9.3] - 2026-09-01

### 🌟 核心亮点 (Highlights)
- **双向快捷切换胶囊体系**：DSH 工作台与控制中心支持快捷胶囊双向切换与 `⌘,` / `Ctrl+,` 全局快捷键。

---

## [v0.9.2] - 2026-09-01

### 🌟 核心亮点 (Highlights)
- **插件中心一体化与凭据识别升级**：市场与已安装总览合并为插件中心；模型凭据支持大写环境变量与别名识别；跨 Profile 导入按插件去重折叠，交互与视觉全面对齐设计规范。

### ✨ 新增功能 (Features)
- **插件中心统一视图**：Profile 管理器视图收敛为单入口，市场与已装插件以子 Tab 承载，全量插件发现与已装管理同屏流转。
- **Release Notes 规范化体系**：建立标准模板 `docs/templates/release-notes-template.md` 与生成规范 `docs/prompts/release-notes.md`；构建期脚本优先从 `docs/RELEASE_NOTES.md` 精准提取版本日志。

### 🛠️ 体验与交互优化 (Improvements)
- **跨 Profile 安装弹窗去重折叠**：同名插件按包名聚合，提供来源 Profile 标签切换器，独立绑定配置复制逻辑，消除多来源平铺歧义。
- **文本截断悬浮提示全量补齐**：插件市场、卡片与概览页面的标题、描述与安装源等截断文本处增加 hover 完整查看。
- **模型凭据卡片防折行优化**：去除重复标题显示，增加状态徽标防换行控制，保持布局紧凑整齐。
- **选择器与表单体验优化**：修复 SelectTrigger 内部元素导致的非预期文本截断，统一「安装源」规范命名并移除多余只读标签。
- **Profile 列表下拉操作文案精简**：下拉菜单操作按钮优化为直观的「重命名」和「删除」，消除多余的冗余前缀。
- **插件市场与前端设计规范对齐**：全面清理前端页面非标准 emoji 与硬编码符号，分类与徽章统一采用 `lucide-react` 矢量图标与设计系统颜色。

### 🐛 缺陷修复 (Bug Fixes)
- **模型凭据**：修复配置了 `refs.DEEPSEEK_API_KEY` 时界面仍提示未配置的识别缺失问题。
- **插件安装选择器**：修复跨 Profile 安装时同名插件重复平铺的问题。
- **Profile 详情**：修复重命名操作的提示文案歧义。

### 🔒 架构、安全与稳定性 (Security & Stability)
- **凭据原子写入**：严格维持 `.credentials.yaml` 的 `0600` 权限与原子写入，保障密钥安全。
- **IPC 收敛**：下线 `reset_profile_dependencies` 入口，依赖自愈回归 pnpm 自动补齐链路，网络面与授权面同步收紧。

---

## [v0.9.1] - 2026-09-01

### 🌟 核心亮点 (Highlights)
- **交互细节与凭据管理体验升级**：跨 Profile 安装插件去重折叠；模型凭据卡片防折行与大写环境变量深度支持；插件市场文本悬浮完整查看与规范字段对齐；全面对齐无 Emoji 严谨设计规范。

### ✨ 新增功能 (Features)
- **大模型凭据环境变量与别名识别**：模型凭据解析支持 `refs.DEEPSEEK_API_KEY`、`OPENAI_API_KEY` 等大写与自定义环境变量格式，准确反映已配置状态。
- **Release Notes 自动化规范体系**：建立标准模板 `docs/templates/release-notes-template.md` 与生成规范 `docs/prompts/release-notes.md`，构建期脚本优先从本文档精准提取更新日志。

### 🛠️ 体验与交互优化 (Improvements)
- **从其他 Profile 安装弹窗去重折叠**：将同名插件按包名聚合，提供来源 Profile 标签切换器，独立绑定配置复制逻辑，消除多来源平铺歧义。
- **文本截断悬浮提示全量补齐**：在插件市场、卡片与概览页面的标题、描述与安装源等截断文本处增加 hover 完整查看。
- **模型凭据卡片防折行优化**：去除重复标题显示，增加状态徽标防换行控制，保持布局紧凑整齐。
- **选择器与表单体验优化**：修复 SelectTrigger 内部元素导致的非预期文本截断，统一「安装源」规范命名并移除多余只读标签。
- **Profile 列表下拉操作文案精简**：下拉菜单操作按钮优化为直观的「重命名」和「删除」，消除多余的冗余前缀。
- **插件市场与前端设计规范对齐**：全面清理前端页面非标准 emoji 与硬编码符号，分类与徽章统一采用 `lucide-react` 矢量图标与设计系统颜色。

### 🐛 缺陷修复 (Bug Fixes)
- **模型凭据**：修复配置了 `refs.DEEPSEEK_API_KEY` 时界面仍提示未配置的识别缺失问题。
- **插件安装选择器**：修复跨 Profile 安装时同名插件重复平铺的问题。
- **Profile 详情**：修复重命名操作的提示文案歧义。

### 🔒 架构、安全与稳定性 (Security & Stability)
- **凭据原子写入**：严格维持 `.credentials.yaml` 的 `0600` 权限与原子写入，保障密钥安全。

---

## [v0.9.0] - 2026-08-31

### 🌟 核心亮点 (Highlights)
- **插件中心与系统控制台重磅上线**：集成社区插件市场、全域插件概览、模型凭据管理、会话自愈与健康诊断。

### ✨ 新增功能 (Features)
- **插件市场与生态接入**：提供精选插件目录、分类过滤、一键安装与版本选择。
- **会话自愈维护**：智能扫描损坏的会话 JSONL 记录，支持单项修复与全量一键自愈。
- **系统控制台**：涵盖大模型凭据、DSH 引擎配置、健康诊断大盘与实时日志查看。

---
